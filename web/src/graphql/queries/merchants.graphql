query Merchants {
  merchants {
    id
    cpname
    imageUrl
    imageAlt
    cplogo
    location
    reviewCount
    averageRating
  }
  allMerchantTags {
    merchantId
    tagId
  }
  tags {
    tagName
    id
  }
}
